hello <%=@user.email%>

<section class="jobs">
  <div class="container">
<ul id="myTab" class="nav nav-tabs">
  <li class="active"><a href="#home" data-toggle="tab">Received Referrals</a></li>
  <li class=""><a href="#profile" data-toggle="tab">Sent Referrals</a></li>
</ul>
<div id="myTabContent" class="tab-content">
  <div class="tab-pane fade active in" id="home">
    <%= render 'received_referrals' %>
  </div>
  <div class="tab-pane fade" id="profile">
    profile tab!
    <%= render 'my_referrals'%>
  </div>
</div>

<% if user_signed_in? %>
   Signed in as <%= current_user.email %>. Not you?
   <%= link_to "Sign out", destroy_user_session_path, :method => :delete %>
   <% end %>
  </div>
<%= render 'layouts/footer' %>

</section>

</br>

