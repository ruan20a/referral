<nav class="navbar navbar-fixed-top" role="navigation">
       <div class="container">
           <div class="navbar-header page-scroll">
               <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-main-collapse">
                   <i class="fa fa-bars"></i>
               </button>
               <a class="navbar-brand" href="http://www.wekrut.com">
                   <span class="light">&nbsp&nbsp&nbspWe</span>Krüt
               </a>
           </div>

           <!-- Collect the nav links, forms, and other content for toggling -->
           <div class="collapse navbar-collapse navbar-right navbar-main-collapse">
              <ul class="nav navbar-nav">
                <li class="page-scroll">
                  <%= link_to 'Jobs', jobs_path %>
                 </li>

                <% if user_signed_in? && current_user.has_enterprise? || admin_signed_in? && current_admin.company.level > 1 %>
                  <li class="page-scroll">
                    <li class="page-scroll">
                      <li><%= link_to "Private Jobs", private_jobs_path %></li>
                    </li>
                  </li>
                <%end %>

                <% if admin_signed_in? %>
                <li class="page-scroll">
                  <li class="page-scroll">
                    <li><%= link_to "Create New Job", new_job_path %></li>
                  </li>
                </li>
                <% end %>

                   <!-- Hidden li included to remove active class from about link when scrolled up past about section -->
           <!-- Collect the nav links, forms, and other content for toggling -->
            <!-- Hidden li included to remove active class from about link when scrolled up past about section -->
                <li class="page-scroll">
                  <% if !user_signed_in? && !admin_signed_in? %>
                    <li class="dropdown"><a href="#" data-toggle="dropdown" class="dropdown-toggle">Login<b class="caret"></b></a>
                      <ul class="dropdown-menu">
                       <li><%= link_to "User", root_path %></li>
                       <li><%= link_to "Recruiter", new_admin_session_path %></li>
                      </ul>
                    </li>
                  <% else %>
                    <li class="dropdown">
                      <a href="#" data-toggle="dropdown" class="dropdown-toggle">
                        ACCOUNT
                        <b class="caret"></b>
                      </a>
                      <ul class="dropdown-menu">
                        <li class="dropdown-menu">
                          <% if user_signed_in? %>
                            <%= render 'layouts/shared/user_links' %>
                          <% else admin_signed_in? %>
                            <% if current_admin.is_main_admin? %>
                              <%= render 'layouts/shared/main_admin_links' %>
                            <% elsif current_admin.is_enterprise? %>
                              <%= render 'layouts/shared/enterprise_links' %>
                            <% else %>
                              <%= render 'layouts/shared/admin_links' %>
                            <% end %>
                          <% end %>
                        </li>
                      </ul>
                    </li>
                <% end %>
              </li>
           </div>
           <!-- /.navbar-collapse -->
       </div>
       <!-- /.container -->
   </nav>

