<nav class="navbar navbar-fixed-top" role="navigation">
       <div class="container">
           <div class="navbar-header page-scroll">
               <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-main-collapse">
                   <i class="fa fa-bars"></i>
               </button>
               <a class="navbar-brand" href="http://www.wekrut.com">
                   <span class="light">&nbsp&nbsp&nbspWe</span>Krüt
               </a>
           </div>

           <!-- Collect the nav links, forms, and other content for toggling -->
           <div class="collapse navbar-collapse navbar-right navbar-main-collapse">
               <ul class="nav navbar-nav">
                   <!-- Hidden li included to remove active class from about link when scrolled up past about section -->
                   </li>

           <!-- Collect the nav links, forms, and other content for toggling -->
          
                   <!-- Hidden li included to remove active class from about link when scrolled up past about section -->
                   
                   
                  
                       
                     
                  <li class="dropdown">
                    <a href="#" data-toggle="dropdown" class="dropdown-toggle">
                      Sign out
                      <b class="caret"></b>
                    </a>
                    
                    <ul class="dropdown-menu">
                     <li>
                      <%= link_to "User Sign out", destroy_user_session_path, :method => :delete  %>
                     </li>
                     <li>
                      <%= link_to "Recruiter Sign out", destroy_admin_session_path, :method => :delete  %>
                    </li>
                  </li>
                   
                   </li>
               </ul>
               <li class="page-scroll">

                      <% if user_signed_in? %>
                        <%= link_to 'Profile', user_path(current_user.id)%>                       
                       <% elsif admin_signed_in? %>
                          <%= link_to 'Recruiter', admin_path(current_admin.id) %>
                       <% else %>
                          <li class="dropdown"><a href="#" data-toggle="dropdown" class="dropdown-toggle">Login<b class="caret"></b></a>
                            <ul class="dropdown-menu">
                             <li><%= link_to "User", new_user_session_path %></li>
                             <li><%= link_to "Recruiter", new_admin_session_path %></li>
                        <% end %>
                      </ul>
           </div>
           <!-- /.navbar-collapse -->
       </div>
       <!-- /.container -->
   </nav>


