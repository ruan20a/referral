$('#all-jobs').append('<%= j render @jobs %>');
<% if @jobs.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @jobs %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>