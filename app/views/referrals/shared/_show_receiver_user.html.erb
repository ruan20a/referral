

<%= @referral.is_admin_notified %>

<%= render 'layouts/flash' %>
<%= render 'layouts/headercandidate' %>

<div class="container" style="margin-top:160px">
  <div class="col-md-8 col-md-offset-2">
    <div class="panel panel-default">
      <div class="panel-heading"><h3 class="panel-title"><strong>Krüt Someone you Know</strong></h3>
      </div>
      <div class="panel-body">
        <div class="form-group">

          <div>
            <strong>Referral Name:</strong>
            <%= @referral.referral_name %>
          </div>

          <div>
            <strong>Referred by:</strong>
            <%= @referral.relationship %>
          </div>

          <div>
            <strong>Referral Email:</strong>
            <%= @referral.referral_email %>
          </div>

          <div>
            <strong>LinkedIn Profile:</strong>
            <%= @referral.linked_profile_url %>
          </div>

          <div>
            <strong>GitHub Profile:</strong>
            <%= @referral.github_profile_url %>
          </div>

          <div>
            <strong>Interest:</strong>
            <%= translate_boolean(@referral.is_interested) %>
          </div>

          <div>
            <strong>Status:</strong>
            <%= @referral.status %>
          </div>

<%= render 'jobs/show' %>

</div>
      </div>
    </div>
  </div>
</div>

<%= render 'layouts/footer_sticky' %>
